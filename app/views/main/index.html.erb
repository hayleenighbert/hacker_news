<h1>Main#index</h1>
<p>Find me in app/views/main/index.html.erb</p>


<%= link_to "New Post", {
	:controller => "main",
	:action => "newpost"
}%>

<% Post.all.each do |p| %>
	<%= p.title %>
	<%= p.link %>

	<% end %>